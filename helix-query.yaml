version: 1

indices:
    home:
        target: /query-index.xlsx
        properties:
            smth:
                select: main > div:nth-of-type(1) > p:nth-of-type(1)
                value: |
                    textContent(el)
